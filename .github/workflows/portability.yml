# Runs the teleop unit tests on macOS and Windows, where the platform
# branches of joy_map's helper spawn execute natively. ROS 2 comes from
# RoboStack's conda packages (GitHub runners ship no ROS); the launch and
# Gazebo suites stay on the Linux build workflow.

name: Portability

on:
  push:
    branches: [caguero/portability]
  workflow_dispatch:

jobs:
  teleop-unit:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v4
      - uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: ros
          condarc: |
            channels:
              - robostack-jazzy
              - conda-forge
          create-args: >-
            ros-jazzy-rclpy
            ros-jazzy-std-msgs
            ros-jazzy-sensor-msgs
            ros-jazzy-ament-index-python
            colcon-common-extensions
            pytest
            pyyaml
            setuptools
      - name: Build the teleop package
        # An isolated workspace: with the siblings present, colcon would
        # try to source the unbuilt dependencies' setup files.
        run: |
          mkdir -p ci_ws/src
          cp -r bluerobotics_teleop ci_ws/src/
          cd ci_ws && colcon build --merge-install
      - name: Run the unit tests
        # Paths computed by Python so they are native on every platform
        # (PYTHONPATH separators and drive letters differ on Windows).
        run: |
          python -c "
          import glob, os
          prefix = os.path.join(os.getcwd(), 'ci_ws', 'install')
          site = glob.glob(os.path.join(prefix, '**', 'site-packages'),
                           recursive=True)[0]
          print(prefix); print(site)
          " > paths.txt
          export AMENT_PREFIX_PATH="$(sed -n 1p paths.txt)"
          export PYTHONPATH="$(sed -n 2p paths.txt)"
          python -m pytest bluerobotics_teleop/test/test_portability.py \
              bluerobotics_teleop/test/test_configs.py -k "not bridge" -v
