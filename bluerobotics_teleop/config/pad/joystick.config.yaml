# Shipped default teleop_twist_joy mapping, shared by every vehicle (a
# vehicle without a motion, like the boat's sway or heave, zeroes it in
# its mixer gains). `ros2 run bluerobotics_teleop joy_map` writes a user
# mapping under $ROS_HOME which the teleop launch prefers over this file.
# ArduSub layout: left stick heave + yaw, right stick surge + sway. RB:
# deadman.
# For a Logitech F310, back switch on X, Mode LED off (joy_node then
# lists the sticks as axes 0, 1, 3, 4 and the D pad hat as 6, 7).
teleop_twist_joy_node:
  ros__parameters:
    axis_linear:
      x: 4
      y: 3
      z: 1
    scale_linear:
      x: 1.0
      y: 1.0
      z: 1.0
    axis_angular:
      yaw: 0
    scale_angular:
      yaw: 1.0
    enable_button: 5
    enable_turbo_button: -1
