# Input side settings for twist_to_thrust (joy_map rewrites this; mixer.yaml is model truth).
twist_to_thrust:
  ros__parameters:
    cmd_timeout_sec: 0.5
    btn_deadman: 5
    axis_epa: 7
    epa_initial: 0.2
    epa_step: 0.1
