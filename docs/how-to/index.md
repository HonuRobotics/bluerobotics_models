# How-to guides

Task oriented guides for configuring, extending and integrating the vehicles.
The examples use the BlueBoat; the same steps apply to the BlueROV2 with
the names swapped (`blueboat` to `bluerov2` in packages, configs and
topics), and the few genuine vehicle differences are called out inline.

```{toctree}
:maxdepth: 1

loadout
installed-vehicle
add-part
add-sensor-part
own-world
rviz
namespaces
faq
```
